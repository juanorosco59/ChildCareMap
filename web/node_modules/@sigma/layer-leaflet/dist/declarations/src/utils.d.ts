import { Map } from "leaflet";
import { Sigma } from "sigma";
export declare const LEAFLET_MAX_PIXEL: number;
export declare const MAX_VALID_LATITUDE = 85.051129;
/**
 * Given a geo point returns its graph coords.
 */
export declare function latlngToGraph(map: Map, coord: {
    lat: number;
    lng: number;
}): {
    x: number;
    y: number;
};
/**
 * Given a graph coords returns its lat/lng coords.
 */
export declare function graphToLatlng(map: Map, coords: {
    x: number;
    y: number;
}): {
    lat: number;
    lng: number;
};
/**
 * Synchronise sigma BBOX with the Map one.
 */
export declare function syncSigmaWithMap(sigma: Sigma, map: Map): void;
/**
 * Synchronise map BBOX with the Sigma one.
 */
export declare function syncMapWithSigma(sigma: Sigma, map: Map, firstIteration?: boolean): void;
/**
 * Settings the min & max camera ratio of sigma to not be over the map's capabilities
 * - Max zoom is when whe can see the whole map
 * - Min zoom is when we are at zoom 18 on leaflet
 */
export declare function setSigmaRatioBounds(sigma: Sigma, map: Map): void;
